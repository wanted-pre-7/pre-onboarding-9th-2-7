const PRICE_VALUE = [1000, 10000, 15000, 25000, 30000];
const SPACE_VALUE = ["강원", "대구", "부산", "서울", "제주"];

const MAX_VALUE = Math.max(...PRICE_VALUE);
const MIN_VALUE = Math.min(...PRICE_VALUE);

export { PRICE_VALUE, SPACE_VALUE, MAX_VALUE, MIN_VALUE };
